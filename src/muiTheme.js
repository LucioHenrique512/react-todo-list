import { createMuiTheme } from "@material-ui/core/styles";

export const DarkTheme = createMuiTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#61DAFB",
    },
    secondary: {
      main: "#69f0ae",
    },
  },
});
